<div class="form-container">
  <h3>Posts Form:</h3>
  <%= form_with model: @post, url: user_posts_path(:user_id), method: :post, local: true, class: "form-container" do |f| %>
    <%= f.label :Title %>
    <%= f.text_field :Title %>
    <%= f.label :Text %>
    <%= f.text_area :Text %>
    <%= f.submit "Add new Post" %>
  <% end %>
</div>